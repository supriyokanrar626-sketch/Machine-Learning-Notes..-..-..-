Random Forest...

We just learned about Decision Trees, a model that predicts by asking a series of if/else questions.
Decision Trees are easy to understand, but there’s a problem: A single tree can sometimes overfit (it memorizes training data and does poorly on new data).
So how can we make predictions more reliable? That’s where Random Forests come in.

1. What is a Random Forest?
A Random Forest is simply a collection of many Decision Trees.

Each tree is trained on a slightly different part of the dataset.
When predicting, all trees “vote” and the majority decision wins.

Analogy:

Imagine asking one friend for advice, they might be wrong.
But if you ask 100 friends and most say the same thing → it’s probably correct.
That’s the idea of Random Forests: wisdom of the crowd.

2. How Random Forests Work

Create many Decision Trees
Each tree sees a random subset of data and features.
Make predictions with all trees
Each tree gives its answer (class).
Take a vote
The most common answer among all trees is the final prediction.

3. Why Use Random Forests?

More accurate than a single Decision Tree.
Less overfitting (since errors from different trees cancel out).
Works well on both classification (Pass/Fail, Spam/Not Spam) and regression (predicting numbers).
4. Example Intuition

image
Predict whether a student will Pass (1) or Fail (0):

Tree 1 → Pass
Tree 2 → Fail
Tree 3 → Pass
Tree 4 → Pass
Final Prediction → Pass (since 3 out of 4 trees voted Pass).

############################

Random Forest MCQ :

A Random Forest predicts whether a student will Pass (1) or Fail (0):

Tree 1 → Pass
Tree 2 → Fail
Tree 3 → Pass
Tree 4 → Pass
Question: What is the final prediction?

ANSWER = The final prediction is Pass (1)...
In a Random Forest, the final decision is determined by Majority Voting. 
Since the individual trees act like a committee, the outcome with the most "votes" wins.

#############################

Random Forest - Worked Example..

Let’s use a Random Forest to predict if a student will Pass (1) or Fail (0)
based on their Study Hours and Attendance.

Step 1: Create the Dataset

We’ll reuse our small student dataset.

import pandas as pd

data = {
    "Study_Hours": [2, 3, 4, 5, 6, 2, 3, 4, 5, 6, 7, 8, 9],
    "Attendance": [60, 65, 70, 75, 80, 62, 68, 72, 78, 85, 88, 90, 95],
    "Pass":        [0, 0, 0, 1, 1, 0, 0, 1, 1, 1, 1, 1, 1]
}

df = pd.DataFrame(data)

X = df[["Study_Hours", "Attendance"]]  # Features
y = df["Pass"]                         # Label

Step 2: Train a Random Forest

We create a RandomForestClassifier with more trees, limited depth, and leaf smoothing so that the model gives 
soft probabilities instead of only [1.0, 0.0].

from sklearn.ensemble import RandomForestClassifier

# Train a random forest with 100 trees, limited depth, and smoothing
model = RandomForestClassifier(
    n_estimators=100, 
    max_depth=3,        
    min_samples_leaf=2,  
    random_state=42      
)
model.fit(X, y)

Explanation of parameters:

n_estimators=100 → The forest will use 100 decision trees (more stable probabilities).
max_depth=3 → Restricts how deep each tree can grow (prevents overfitting).
min_samples_leaf=2 → Ensures each leaf has at least 2 samples, smoothing probability estimates.
random_state=42 → Fixes randomness, so you get the same results every time.
Step 3: Make Predictions

Now let’s predict on the training data.

predictions = model.predict(X)
print("Predictions:", predictions.tolist())

Output

Predictions: [0, 0, 0, 1, 1, 0, 0, 1, 1, 1, 1, 1, 1]
The Random Forest predicts correctly for all students.

Step 4: Probability of Predictions

Random Forests can also tell us the probability (how many trees voted for each class).

probabilities = model.predict_proba(X)
for row in probabilities:
    fail, pass_ = row
    print(f"[{fail:.1f} {pass_:.1f}]   # {int(fail*100)}% Fail, {int(pass_*100)}% Pass")

Example output:

[1.0 0.0]   # 100% Fail, 0% Pass
[1.0 0.0]   # 100% Fail, 0% Pass
[0.8 0.2]   # 80% Fail, 20% Pass
[0.0 1.0]   # 0% Fail, 100% Pass
[0.0 1.0]   # 0% Fail, 100% Pass
[1.0 0.0]   # 100% Fail, 0% Pass
[1.0 0.0]   # 100% Fail, 0% Pass
[0.4 0.6]   # 40% Fail, 60% Pass
[0.0 1.0]   # 0% Fail, 100% Pass
[0.0 1.0]   # 0% Fail, 100% Pass
[0.0 1.0]   # 0% Fail, 100% Pass
[0.0 1.0]   # 0% Fail, 100% Pass
[0.0 1.0]   # 0% Fail, 100% Pass

################  FULL CODE ############

import pandas as pd
from sklearn.ensemble import RandomForestClassifier

# 1. Create the Dataset
data = {
    "Study_Hours": [2, 3, 4, 5, 6, 2, 3, 4, 5, 6, 7, 8, 9],
    "Attendance": [60, 65, 70, 75, 80, 62, 68, 72, 78, 85, 88, 90, 95],
    "Pass":        [0, 0, 0, 1, 1, 0, 0, 1, 1, 1, 1, 1, 1]
}

df = pd.DataFrame(data)
X = df[["Study_Hours", "Attendance"]]
y = df["Pass"]

# 2. Train a Random Forest
model = RandomForestClassifier(
    n_estimators=100, 
    max_depth=3,        
    min_samples_leaf=2,  
    random_state=42
)
model.fit(X, y)

# 3. Make Predictions
predictions = model.predict(X)
print("Predictions:", predictions.tolist())

# 4. Probability of Predictions
print("\nProbabilities per student:")
probabilities = model.predict_proba(X)
for row in probabilities:
    fail, pass_ = row
    print(f"[{fail:.1f} {pass_:.1f}]   # {int(fail*100)}% Fail, {int(pass_*100)}% Pass")

#########################
